---
title: Example Simulation
section: Simulation
order: 6
description: A worked simulation chapter demonstrating the full run anatomy. Duplicate per simulation.
---

<ScreenshotPlaceholder caption="How the example simulation works. Replace with a real opening image." />

Replace this with a description of one simulation: the what-if it runs, the event it takes as input, and the lead time it gives the operator. This chapter demonstrates the full anatomy of a simulation page, duplicate it per simulation.

Every run follows the same arc. You set a trigger, the model forecasts the effect, and it proposes a response. Reading a run is reading that chain.

## Before running

<ScreenshotPlaceholder caption="The simulation before a run: the left panel builds the scenario, the right panel shows live readings, and a Run button starts it. Replace with a real screenshot." />

Describe what the operator sets before running: the event to simulate, the volume or intensity, and any variables that tune the assumptions.

| Input | What it sets |
|---|---|
| Event to simulate | The trigger being modelled. |
| Volume | The scale of the scenario. |
| Intensity | How the volume arrives over time. |

## After running

<ScreenshotPlaceholder caption="The forecast after a run. Replace with a real screenshot." />

Describe what the operator reads in the forecast and how they act on it.

<Callout type="warning">
Replace with any caveat specific to this simulation, for example the confidence of the forecast or a known limitation.
</Callout>
